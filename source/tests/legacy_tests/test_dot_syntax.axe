use std.string;

model Person {
    name: string;
    age: i32;
}

def main() {
    parallel for mut i = 0 to 10 {
        val person = new Person(name: "Alice", age: i);
        println person.age;
    }
}
