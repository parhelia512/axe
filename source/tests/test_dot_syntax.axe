use std.string;

model Person {
    name: string;
    age: i32;
}

main {
    parallel for mut i = 0 to 10 {
        mut person = new Person(name: "Alice", age: i);
        println person.age;
    }
}
