// use std.io;

enum State {
    RUNNING,
    CHANGING,
    STOPPED
}

def main() {
    mut val fixed_state: State = State.CHANGING;
    // println State.RUNNING;
    // println fixed_state;
    fixed_state = State.RUNNING;
}
