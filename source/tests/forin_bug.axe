use stdlib/arena(Arena);
use stdlib/os(get_cmdline_args);
use stdlib/io(print_str);

main {
    mut arena: Arena = Arena.create(65536);
    for arg in get_cmdline_args(ref_of(arena)) {
        print_str(arg);
        print " ";
    }
}